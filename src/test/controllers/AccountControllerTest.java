

/*
 * This would take a tad bit longer then im willing to spend on what is essentially homework no one asked for.
 * The unit test would be in the same structure as the java classes.
 * Tests for controllers, services, gateways, and repositories. The tests would consisit of happy paths
 * and errors raised and some common edge cases, like empty/null values.
 *
 */